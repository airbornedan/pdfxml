# Core runtime dependencies -- shared by every install target. The other
# requirements-*.txt files pull this in with `-r` and add to it;
# requirements.lock is the pinned, hashed transitive closure.

# Core web framework
Flask==3.1.3
Werkzeug==3.1.8

# CSRF protection
Flask-WTF==1.3.0

# PDF parsing/rendering -- the only PDF-handling library in this app,
# deliberately: no Ghostscript, no ImageMagick, no poppler. Self-contained,
# no external binaries.
PyMuPDF==1.26.5

# XML fragment construction -- safe escaping/serialization, not string formatting
lxml==5.4.0

# Markdown -> HTML for the Process/Troubleshooting content (content/*.md).
# Pure Python, no dependencies.
mistune==3.3.4

# Resolves a writable per-install data directory for the frozen desktop
# build, whose own directory may be read-only or inside the bundle.
platformdirs==4.11.5
