[pytest]
testpaths = tests
addopts = -q
markers =
    big_image: image-crop test that loads an oversized source image
filterwarnings =
    ignore:builtin type .* has no __module__ attribute:DeprecationWarning
