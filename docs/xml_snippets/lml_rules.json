{
  "name": "LML rules",
  "version": 1,
  "source": "docs/xml_snippets/LML_rules.txt",
  "tags": {
    "structure": [
      "section",
      "title",
      "para",
      "orderedlist",
      "itemizedlist",
      "listitem",
      "informaltable",
      "thead",
      "tbody",
      "tr",
      "th",
      "td",
      "colgroup",
      "col"
    ],
    "media": [
      "mediaobject",
      "imageobject",
      "imagedata",
      "informalfigure",
      "fileref"
    ],
    "inline": [
      "emphasis",
      "guilabel",
      "xref",
      "indexterm",
      "primary",
      "secondary"
    ],
    "admonitions": [
      "note",
      "warning",
      "caution"
    ]
  },
  "rules": [
    {
      "id": "section-root",
      "kind": "structure",
      "description": "The topic root is always <section>."
    },
    {
      "id": "section-title",
      "kind": "structure",
      "description": "Each <section> requires exactly one <title>, and that title must be the first non-whitespace child."
    },
    {
      "id": "lists-content",
      "kind": "structure",
      "description": "Every <orderedlist> or <itemizedlist> must contain at least one <listitem>; each list item must have real content."
    },
    {
      "id": "listitem-content",
      "kind": "structure",
      "description": "A <listitem> may contain only <para>, <mediaobject>, and nested <orderedlist>/<itemizedlist> elements as direct children -- no bare text and nothing else directly inside it. At least one of these is required; multiple are allowed (for example a <para> followed by a sublist). Treated as the complete allowed set for now -- covers the cases interns actually hit.",
      "allowed_children": ["para", "mediaobject", "orderedlist", "itemizedlist"],
      "min_children": 1
    },
    {
      "id": "listitem-not-empty",
      "kind": "structure",
      "severity": "warning",
      "description": "A <listitem> must not be empty (no content at all)."
    },
    {
      "id": "listitem-empty-para",
      "kind": "structure",
      "severity": "caution",
      "description": "A <listitem> should not contain an empty <para> (a <para> with no text or inline content). Softer than a warning -- structurally valid, but worth flagging."
    },
    {
      "id": "imageobject-mediaobject",
      "kind": "media",
      "description": "Every <imageobject> must be nested inside a <mediaobject>."
    },
    {
      "id": "mediaobject-imagedata",
      "kind": "media",
      "description": "A <mediaobject> must include <imageobject> and <imagedata>, and <imagedata> must include fileref."
    },
    {
      "id": "table-structure",
      "kind": "structure",
      "description": "A table must contain <tbody>, each row must be a <tr> with <td> or <th> cells, and bare text inside a cell is not allowed."
    },
    {
      "id": "table-border-pairing",
      "kind": "structure",
      "description": "An <informaltable>'s frame and rules attributes, when present, must be one of exactly two paired combinations: frame=\"box\" rules=\"all\" (a bordered table) or frame=\"void\" rules=\"none\" (a borderless layout table, e.g. image-and-text side by side). No mixing -- box with none, void with all, or any other combination is invalid; pick one type per table. The attributes are not required, but a table with neither is worth a nudge -- a real data table (one with a <thead>) usually wants borders.",
      "allowed_pairs": [
        {"frame": "box", "rules": "all"},
        {"frame": "void", "rules": "none"}
      ],
      "on_mismatch": "warning",
      "on_missing": "caution"
    },
    {
      "id": "admonitions-para",
      "kind": "admonitions",
      "description": "An admonition (<note>, <warning>, <caution>) may contain only <para> elements as direct children -- nothing else (no lists, tables, or media) is allowed directly inside it. At least one <para> is required; multiple are allowed. Inline/block content within those <para> tags follows the normal <para> content rules.",
      "allowed_children": ["para"],
      "min_children": 1
    },
    {
      "id": "inline-only",
      "kind": "inline",
      "description": "Inline-only elements such as <emphasis role=\"bold\"> and <guilabel> are allowed only where the rules permit them."
    },
    {
      "id": "error-conditions",
      "kind": "errors",
      "description": "Current error conditions include missing titles, misplaced titles, missing list items, missing mediaobject/imageobject/imagedata, raw text in cells, and missing paragraph content in admonitions."
    },
    {
      "id": "warning-conditions",
      "kind": "warnings",
      "description": "Current warning conditions include unknown elements, empty paras, empty notes, empty list items, inconsistent table column counts, multiple title elements in the same section, and unknown inline role values."
    }
  ],
  "notes": [
    "This JSON file is the source of truth for the rule catalog -- rules are written directly here, not in a separate prose doc.",
    "Severity scale (rules opt in via a \"severity\" field; absent means treat as an error for now): error (blocks save / structurally broken) > warning (real problem, not fatal) > caution (structurally valid, still worth flagging)."
  ]
}
